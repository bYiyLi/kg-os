# KG OS 会话交接

更新时间：2026-09-08

本文只记录接续位置与工作规则，不复制 Schema 规则、技术选型或研究结论，不作为设计真源。

先读 [README](README.md) 和 [设计状态导航](docs/design.md#设计状态导航)。需要形成过程时再读 `docs/background.md`，需要外部参考时再读 `docs/research/industry-landscape.md`。

## 当前状态

真实本地仓库：`/Users/yi/Code/KGOS`

本次整理时仓库只有文档，没有业务实现。技术架构与 Ontology Schema 基础规则已确认，具体结论以 `docs/design.md` 为准。

新会话涉及仓库事实时，应通过正式版 `Nexum` 连接 `Yi Mac` 并检查当前真实状态，不依赖本文件中的历史状态。

## 接续位置

当前聚焦本体；时序／版本的详细设计暂不展开。本体已有规则见设计文档的 `Ontology Schema`，包括 Schema 身份、可选存在、创建／修改／删除与标签变化时的强一致规则。剩余工作分别见 `待设计能力合同` 与 `工程实现待办`。

不再把“Schema 是否也是图”“是否需要独立类型空间”“如何按名称绑定”“多个 Schema 是否同时生效”作为未决问题。这些已有明确结论，不因缺少表结构、索引或校验代码而重开。

## 工作规则

- 提出下一步前，先定位 `docs/design.md` 中的相关决定。已确认规则直接沿用；只有用户要求调整，或发现有具体证据的冲突时，才提出重新评估。
- 工程细节由开发者先给出方案，不把实现待办重新包装成产品问题；真实语义缺口用具体例子和推荐方案说明，不让用户从头重做设计。
- 获准记录新增或变更设计时，先更新设计真源，并在同次文档变更中同步受影响的 README 与本文件；摘要只引用，不另立规则。Noven 与外部研究只能提供参考，不能自动成为 KG OS 决定。
- 讨论和评审默认只读；修改、实现、提交、推送分别按用户授权执行。实现遵循：`检查现状 → 判断最小方案 → 修改 → 验证 → 检查 diff → 汇报`，分别报告已确认、已修改、已验证、已提交和已推送。

如果设计真源无法读取，应说明未完成核对，不用旧交接内容猜测当前设计。

## 新会话开场

> 通过正式版 Nexum 连接 Yi Mac 并打开 `/Users/yi/Code/KGOS`。读取 `README.md`、`docs/design.md` 和 `HANDOFF.md`，核对当前 Git 与文件状态。沿用已确认设计，区分暂定方案、待设计合同与工程实现待办；针对当前本体阶段提出一个不重复已确认事项的下一步方案。先只讨论，不修改文件或实现代码。
