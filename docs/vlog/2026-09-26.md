# 2026-09-26

## 15:00 — Phase 09 v0.1.0 Release acceptance 与 Trusted Publishing 安全收尾

Scope and authorization: 延续已授权的 `v0.1.0` 正式发布与 Phase 09安全收尾；用户随后明确授权“提交并推送，持续到 Phase 09 `done`”。本轮执行真实 npm/GitHub状态复核、六包 Trusted Publisher配置、bootstrap credential清理、长期 OIDC workflow最小修正、文档同步、commit / push与远端 CI closure。

Observed Release evidence:

- `v0.1.0` 本地与远端 tag均指向原 release revision `d35e254e1b486aa2d8597e68d23a6e4efc1990c3`，未移动；
- Release recovery Run `36219384704` 已 `completed/success`，完成 preflight、四平台 candidate、aggregate、publish verification、四平台 public-registry `npx` smoke与 GitHub Release；
- GitHub Release `KG OS v0.1.0` 已发布，draft=false、prerelease=false；
- `@kgos/sdk`、`@kgos/cli` 与四个 Runtime package均为 public `0.1.0`，六包 `latest=0.1.0`。

Trusted Publishing / credential cleanup:

- 六包逐包配置 GitHub Actions Trusted Publisher：owner/repository=`bYiyLi/kg-os`，workflow=`release.yml`，允许直接 `npm publish`；
- 六包 publishing access均收紧为“Require two-factor authentication and disallow bypass 2fa tokens”；
- 六包逐包重新读取 npm settings验证 Trusted Publisher identity与严格 publishing access均成立；
- GitHub repository Actions secret `NPM_TOKEN` 已删除，Secrets页面显示 repository secrets为空；
- npm granular token `kgos-v0.1.0-bootstrap` 已删除，Access Tokens页面报告 `deleted 1 token` 且当前token列表为0。

Repository closure:

- `.github/workflows/release.yml` 的长期 publish step移除 `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}`，保留 `id-token: write` 与 npm registry setup，使后续正式 Release直接使用 npm Trusted Publishing / OIDC；
- `scripts/release-core.test.mjs` 增加静态断言，禁止 Release workflow重新引入 `NPM_TOKEN` / `NODE_AUTH_TOKEN`；
- `docs/design/client.md` 同步长期 OIDC合同：正式 publish step不注入长期 token；
- npm官方当前 Trusted Publishing要求 npm CLI >= 11.5.1、Node >= 22.14.0；仓库 Node 24.15.0满足该要求。当前六包配置已验证，但 `v0.1.0` 本身使用bootstrap token完成首次publish；由于published version immutable，本轮不创建额外版本只为重复测试OIDC，下一次正式Release将首次真实执行无token OIDC publish。

Status truth:

- 公开 `v0.1.0` Release acceptance与安全收尾已完成；
- 长期 OIDC closure提交 `26fd22f26967ee429ec982c37e5871e7dbc12f5b` 已通过 SSH push 到 `main`；原 HTTPS OAuth凭据缺少 `workflow` scope而被 GitHub拒绝修改 workflow，开发机现有 GitHub SSH credential随后成功完成同一 push；
- GitHub Actions CI Run `36226930183` 已完成，Validate `108362650639` 与四个平台 Native Runtime jobs（darwin-x64 `108362650701`、linux-arm64 `108362650707`、linux-x64 `108362650727`、darwin-arm64 `108362650754`）全部 `success`；
- Phase 09全部完成条件满足，状态正式进入 `done`。

## 22:13 — Phase 10 Runtime & CLI Hardening 设计与开发计划

Scope and authorization: 用户授权把真实 `v0.1.0` CLI 使用验收暴露的问题整理进 KG OS 当前设计真源，并新建 Phase 10 开发计划；本轮只维护文档与研究证据，不实现代码、不提交、不推送、不发布。

Confirmed design:

- D78 冻结 operation-scoped Branch：pooled write connection 归还前必须证明 autocommit、无 active transaction、checkout `main`；成功、错误、取消、stream failure / early-close都适用，无法证明时 discard connection，后续 Evolution 行为不得依赖 pool 随机 connection；
- D79 冻结 stale daemon recovery：unreachable locator 只触发 contender，daemon-held OS exclusive lock 是唯一 ownership 仲裁；stale winner自动覆盖 locator，live owner仍持lock时 fail closed，`doctor` 继续 side-effect-free；
- D80 冻结 new Instance 默认 `jieba`：省略 `--fulltext-analyzer` 不prompt并显式写入config；Jieba由 KG OS Runtime official artifact提供，Lithograph继续保持通用FTS5边界；caller extension先加载、official Jieba最后注册，固定最终 `jieba` identity；
- CLI help改为逐层可发现，single-JSON-document mode统一支持 `--pretty`；`@kgos/cli` Node baseline保持 `>=24.15.0`；Provider HTTP diagnostics必须由实际owner修复，KG OS不做字符串补丁。

Planning / research:

- 新建 `docs/development/phases/10-runtime-cli-hardening.md`，状态 `ready`；Phase 10只交付hardening candidate，不自动包含npm publish、Git tag或GitHub Release；
- 新建 `docs/research/jieba-tokenizer.md`。已调研一个MIT OR Apache-2.0候选；macOS arm64 repo外构建可编译Rust crate，但按上游文档路径没有产出可直接给SQLite加载的动态库，进一步显式动态库构建尝试也未形成可接受的稳定发布路径，因此没有把候选项目名冒充为已选定official artifact；exact implementation由10.3在既定四平台、license、artifact identity与真实load/query acceptance内自行选择；
- 修正 `docs/design.md` 中Phase 09残留的旧 `blocked` 状态，当前真实基线保持Phase 00–09 `done`、v0.1.0已发布、Phase 10 `ready`但尚未实现。
- README同步Node.js >=24.15.0正式前置、Phase 10当前 `ready` 状态、D78–D80入口与Phase 10计划链接；没有把尚未实现的hardening写成当前公开版本能力。

Verification:

- 默认shell的Node 22.22.0按仓库 `engines.node` 正确拒绝运行pnpm门禁；随后使用仓库pinned Node 24.15.0重新执行验证；
- `pnpm check:markdown`：通过，43个Markdown文件0 issue；
- `pnpm check:spelling`：通过，134个文件0 issue；
- `git diff --check`：通过；
- diff-scoped Markdown local link检查：13个本次改动Markdown文件共检查409个本地链接/锚点，全部存在；检查器同时识别显式HTML `id`和GitHub heading slug；
- 文档多轮review已闭环Phase状态、Jieba最终registration顺序、历史D51/D52/D76 supersession、CLI stale recovery表述与真实用户验收证据混用等finding。

Current state: 仅文档工作树有修改；Phase 10代码尚未开始，未提交、未推送、未发布。reviewed documentation scope当前无剩余task-affecting finding。
