# 2026-09-25

Timezone: UTC

## 12:12 — Phase 09 MVP Release Closure 仓库内实现与 Review 收口

Scope and authorization: 用户要求按照当前 KG OS 规范完成 Phase 09 代码开发，并持续执行 review → 修复 → review，直到当前可执行范围没有剩余问题。本轮授权包含实现、测试、验收与必要文档同步；不包含 commit、push、真实 npm publish、Git tag、GitHub Release 或其它对外发布动作。

### Decisions

- 首个公开 MVP Release baseline 按现有 Client/CLI/Runtime 设计示例与版本合同冻结为 exact `0.1.0`，正式 npm dist-tag 为 `latest`；不增加第二个 version source。
- Phase 09 的真实发布仍遵守阶段原 Acceptance：六包 public registry、四平台 registry `npx`、dist-tag promotion、Git tag 与 GitHub Release 都必须有实际证据，仓库内 tooling 通过不能替代这些外部结果。
- 当前本机 `npm whoami` 明确报告未认证，没有 `@kgos` scope publish authority 证据，因此 Phase 状态保持 `blocked`，不静默更换 package namespace。

### Changes

- 同步 root、SDK、CLI、Web、四个 Runtime package、SDK public constant 与 Go build info 到 `0.1.0`，并保持 CLI 对 SDK / Runtime 的 exact same-version dependency。
- 新增手工 `workflow_dispatch` Release workflow 与 release tooling：clean/source/version/tag preflight、npm authority、四平台 immutable candidate、candidate aggregation、Runtime manifest/native SHA-256、npm SRI、partial publish recovery、exact registry verification、四平台 clean public-registry `npx` smoke、`latest` promotion、GitHub Release finalization。
- Review 中补强同 revision/version 绑定、四个平台 SDK/CLI candidate artifact identity、非 `main` fail-closed、staging/final dist-tag 隔离、CLI dependency publish ordering、正式 tag 防提前推进，以及 packed/registry smoke daemon cleanup。
- README、设计状态导航、开发路线、Phase 09 与开发指南同步到“release engineering 已实现、真实 Release 仍 blocked”的实际状态。

### Verification

Source state and environment: 本地 `main` 比 `origin/main` 超前 Phase 09 计划提交，当前 Phase 09 实现仍为未提交工作树；验证绑定该 tracked/untracked 工作树，不把它冒充 pushed Release revision。
Commands, scope, and observed results: targeted `release-core` tests、syntax、ESLint、Prettier、version consistency 与当前平台 `0.1.0` packed candidate smoke成功；主工作树 `git diff --check && pnpm validate` 成功；独立 fresh-source 从 `HEAD + 当前 tracked/untracked Phase 09 变更` 重建后 `pnpm run setup && pnpm validate` 成功，覆盖既有 Go/TS quality、race/coverage/security、Playwright、真实 Lithograph v0.3.0 native、package/license/audit/diff gates。

### Remaining

- 真实 `@kgos` npm publish authority 尚无证据；六包 public publish、四平台 post-publish registry smoke、`latest` promotion、`v0.1.0` tag 与 GitHub Release 均未执行。
- 当前 reviewed repository scope 没有剩余 task-affecting code finding；Phase 09 只有在上述外部 Acceptance 与最终发布状态文档同步真实完成后才能进入 `done`。

## 13:35 — Phase 09 改为 Git Tag 触发 Release

Scope and authorization: 用户确认把正式 Release 改为“push `v<version>` tag → GitHub Actions 自动发布”，并要求遇到必须由本人完成的 npm 认证、2FA、credential 或权限动作时暂停说明。本轮授权包含对应设计、workflow、release tooling、测试与文档修改；不把未明确执行的 commit、push、Git tag、npm publish 或 GitHub Release 记为已完成。

### Decisions

- Git tag 从“registry acceptance 后生成的结果”改为正式 Release 指令与 source identity；普通 PR / `main` push 不 publish，`workflow_dispatch` 只恢复一个已经存在的 release tag。
- Release preflight要求 tag 为 exact `v<package-version>`、指向当前 checkout revision，且 revision 属于 `origin/main` 历史；这样既支持正常 tag push，也支持 main 后续前进后的 same-tag recovery。
- 为兼容 npm Trusted Publishing / OIDC，取消 post-publish `npm dist-tag add` promotion。四个 Runtime → SDK → CLI 最后按正式 `latest` 直接 publish；CLI 发布前验证所有 exact dependency 已存在，partial recovery 只接受 integrity 与正式 dist-tag 都匹配的 immutable artifact。
- npm Web 已真实确认 `@kgos` organization存在，当前 npm account 为 owner 且启用 2FA。六个 package 尚不存在，因此首次 publish 使用短期 bootstrap credential；package 建立后为六包配置 GitHub Actions Trusted Publisher，再删除 bootstrap secret。

### Changes

- `.github/workflows/release.yml` 改为 `v*` tag push 自动触发，并保留需要 existing tag input 的 recovery dispatch；publish job加入 OIDC `id-token: write`，finalize 只在 registry smoke 成功后为已有 tag 创建/验证 GitHub Release。
- release tooling 改为 tag/version/main-history preflight、direct final-tag publish 与 CLI-last partial recovery，删除 staging-tag / promote 命令路径。
- 六个 public npm package 增加与 `bYiyLi/kg-os` 一致的 repository metadata，作为 Trusted Publishing identity。
- Client设计、Phase 09、开发路线、README与开发指南同步新的 release ownership / bootstrap / OIDC 合同。

### Remaining

- 需要完成本轮代码验证与 final review；当前修改尚未提交或推送。
- 首次 Release 仍需由用户创建短期 npm bootstrap credential 并安全配置为 GitHub repository secret；之后才能创建并 push `v0.1.0` tag触发真实发布。

### Verification

- Release targeted tests：`scripts/release-core.test.mjs` 7/7 通过；workflow 静态检查覆盖 `v*` tag trigger、existing-tag recovery、OIDC `id-token: write`、checkout revision truth、CLI-last publish 与无 promote job。
- 在独立临时 clone 中模拟 release tag preflight：`v0.1.0` 与 package version / revision 一致时通过；`v0.2.0` 被 fail closed；真实仓库未创建任何 release tag。
- 主工作树 `pnpm validate` 完整通过：Go race、90.0% statement coverage、govulncheck、82 个 Vitest、TS coverage、type coverage、duplication/unused/version/dedupe、build、exports、Playwright、Lithograph native、packed package smoke、licenses、npm audit 与 diff gate 全部成功。
- package metadata 调整后的 `pnpm check:exports` 对 SDK 与 CLI 均为 `All good!`。
- 第一次 fresh-source setup 因 `release-assets.githubusercontent.com` 在 TLS 建连前发生 connection reset 而失败，分类为外部网络环境错误；未把该次运行记为通过。
- 第二个全新临时 clone 从当前最终 diff 重新执行 `pnpm run setup && pnpm validate`，完整 exit 0，覆盖最终 repository metadata 与 recovery revision 修复。
- Final review 发现并修复 recovery workflow 不能使用 dispatch event 的 `GITHUB_SHA` 作为 release revision 的问题，现改为 checkout 后 `git rev-parse HEAD`；对应静态回归测试已通过。
- `git diff --check` 通过，真实仓库不存在 `v0.1.0` tag；reviewed scope 当前没有剩余 task-affecting engineering finding。

### Remaining after verification

- 本轮 Tag-triggered Release 调整仍是未提交、未推送状态。
- 首次 `0.1.0` 仍需短期 npm bootstrap credential；credential 配置完成且 release revision 已推送后，才可显式创建并 push `v0.1.0` 触发真实 public Release。
- 六包首次发布成功后，还需逐包配置 GitHub Actions Trusted Publisher，并删除 bootstrap `NPM_TOKEN`，再以真实 registry / smoke / GitHub Release 证据决定 Phase 09 是否进入 `done`。

## 22:54 — v0.1.0 首次 Release aggregate recovery

Scope and authorization: 用户已明确授权正式发布 `v0.1.0`。Tag 已 push 到已验证 revision，Release run 继续按 Phase 09 fail-closed 路径执行；发布过程中发现的 repository-side release engineering defect 在同一发布授权范围内最小修复，不移动已存在 release tag。

### Observed release state

- `v0.1.0` 远端 tag 指向 `d35e254e1b486aa2d8597e68d23a6e4efc1990c3`。
- Release run `36149542564` 的 preflight 与四个平台 candidate 全部成功。
- Aggregate job 在 npm publish 前失败；publish / registry smoke / GitHub Release 全部被 dependency chain 跳过，因此 registry 尚未产生本次六包 release。
- 四个平台 artifact 对比确认 SDK tarball 完全一致；CLI tarball 在 macOS 两目标间一致、Linux 两目标间一致，跨 OS 唯一内容差异是 `package/dist/.tsbuildinfo`。除该 TypeScript incremental build cache 外，CLI tarball 文件内容、mode 与实际运行/类型产物一致。

### Recovery fix

- CLI `tsBuildInfoFile` 改到 `.cache/tsbuildinfo/cli.tsbuildinfo`，与 SDK 的 cache ownership 一致。
- release pack staging 无条件删除 `dist/.tsbuildinfo`，并把该路径加入 SDK/CLI tarball forbidden-content 验证。
- Release candidate workflow 在 pack 前显式删除旧 Tag checkout 可能生成的 `dist/.tsbuildinfo`，使 `workflow_dispatch` 能在不移动 `v0.1.0` tag 的前提下恢复同一 source release。

### Verification

- Release targeted test `scripts/release-core.test.mjs` 7/7 通过。
- `pnpm build` 后确认 `packages/cli/dist/.tsbuildinfo` 不再存在，`.cache/tsbuildinfo/cli.tsbuildinfo` 正常生成。
- `pnpm check:package` 成功；SDK/CLI tarball forbidden-content gate确认不包含 `package/dist/.tsbuildinfo`，packed runtime smoke成功。
- `pnpm check:quick`、Markdown/spelling/secret scan 与 `git diff --check` 成功，82 个 Vitest全部通过。
